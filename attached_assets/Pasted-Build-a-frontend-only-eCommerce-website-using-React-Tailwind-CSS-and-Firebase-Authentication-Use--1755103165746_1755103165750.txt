Build a frontend-only eCommerce website using React, Tailwind CSS, and Firebase Authentication. Use Firestore optionally for storing and retrieving product data. The app should be modular, responsive, and scalable. Use React functional components and hooks.

🛠 Tech Stack:
- React (functional components, hooks)
- Tailwind CSS (styling)
- React Router (routing)
- Firebase Authentication (email/password login)
- Firestore (optional: for product data)
- Context API (for cart state)
- Vite (preferred setup)

📄 Pages:
1. Home Page:
   - Hero section
   - Featured products (from Firestore or mock JSON)
   - Categories grid

2. Product Listing Page:
   - Grid layout
   - Filters (category, price)
   - Search bar

3. Product Detail Page:
   - Image gallery
   - Description, price
   - Add to cart

4. Cart Page:
   - List of items
   - Quantity adjustment
   - Total price

5. Checkout Page:
   - Shipping form
   - Cart summary

6. Login/Register Page:
   - Firebase email/password auth
   - Form validation
   - Redirect after login

7. Profile Page (optional):
   - Show user info
   - Logout button

🧩 Components:
- Navbar: Logo, links, cart icon, login/logout
- Footer: Contact info, links
- ProductCard: Image, title, price
- Modal: Quick product preview
- Toast: Cart actions

📦 Functionality:
- Firebase Auth: login, register, logout
- Firestore (optional): fetch products
- Cart: add/remove, adjust quantity
- Persist cart in localStorage
- Responsive design
- Basic form validation

📁 Structure:
- `/components`: UI components
- `/pages`: Route views
- `/contexts`: Cart and Auth context
- `/firebase`: Firebase config and services
- `/data`: Mock data if Firestore not used
- `/assets`: Images/icons

📄 Firebase Setup:
- Initialize Firebase app
- Set up Firebase Auth (email/password)
- Optional: Firestore collection `products`
- Use `onAuthStateChanged` to manage user session

Generate the full project structure with sample Firebase config, mock product data, and clean modular code. Prioritize usability, responsiveness, and clarity.